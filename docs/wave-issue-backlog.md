# Wave Issue Backlog

These are good candidate issues to publish before applying to a maintainer program like Wave.

## Good first issues

1. Add logout confirmation and session timeout copy
2. Add inline validation for transfer recipient email
3. Add empty-state guidance when a filtered transaction view has no results
4. Add a reset-data button in the frontend that calls a local backend route
5. Add tests for duplicate email registration and failed transfers

## Medium maintenance issues

1. Add pagination and page size controls to `GET /transactions`
2. Add backend request logging with environment-based verbosity
3. Add account switching UI for the seeded local demo users
4. Add JSON export for the transaction ledger
5. Add an admin-only local route to inspect all demo wallets for debugging

## Maintainer notes

For each issue you publish:

- define the exact files likely to change
- describe expected behavior after completion
- note whether tests are required
- say whether UI screenshots are helpful
