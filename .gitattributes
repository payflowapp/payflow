# Normalize line endings: store as LF in the repo, check out as-is on each OS.
* text=auto eol=lf

# Treat these as binary so they are never line-ending normalized.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text
